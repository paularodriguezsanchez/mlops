# Núcleo ML (algoritmos estándar, alcance del anteproyecto)
scikit-learn==1.7.2
pandas==2.3.3
numpy==2.2.6
scipy==1.15.3
joblib==1.5.3
pyarrow==24.0.0

# MLOps
mlflow==3.16.0
evidently==0.7.21
dvc==3.67.1

# Ranking (núcleo del proyecto — priorización, no solo clasificación binaria)
lightgbm==4.7.0

# Explicabilidad (P1)
shap==0.49.1
matplotlib==3.10.9

# Cliente/serving
requests==2.34.2
flask==3.1.3
gunicorn==23.0.0
tabulate==0.9.0

# Calidad
pytest==9.1.1
ruff==0.15.20
pyyaml==6.0.2

# Pin de seguridad de una dependencia transitiva (mlflow, evidently y
# google-auth la arrastran): 48.0.1 acumula PYSEC-2026-3552/3553/3554. Los tres
# se resuelven en la serie 50, que solo admite mlflow 3.16 en adelante: ese es
# el motivo de subir mlflow. Ver docs/SECURITY_AUDIT.md.
cryptography>=50.0.0,<51
